1、form对象是个数组，负责存储表单中所有域，但它的数组元素并非利用数值索引存储，而是使用域中独有name属性设定的标识符。

2、onfocus在元素或者表单域取得输入焦点时触发，onblur则是域失去焦点时被触发。

3、js透过内置RegExp对象支持正则表达式，使用RegExp对象的test（）方法用于对字符串应用正则表达式模式的检测

4、电子邮箱的正则表达式：/^[\w_\+\.-]+@[/w-]+(\.\w{2,3})+$/